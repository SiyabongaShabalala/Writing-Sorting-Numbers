# Writing-Sorting-Numbers
This program reads numbers from a CSV file, stores them in an array, and allows the user to choose whether to sort them in ascending or descending order. It then uses `qsort()` to sort the numbers and writes the sorted results to a new output file.
